# Elaborar un script que muestre la carpeta en la que se encuentra el programa de Python actual

import os as cmd    # Importar la libreria del sistema operativo
print(cmd.getcwd()) # Imprimir la ruta del directorio de trabajo actual
